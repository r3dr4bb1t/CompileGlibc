

1. git clone glibc 후, 2.26.9000 브랜치로 check out

2. stdlib-common 의 printf 에 write을 추가하여 print 전에
write 을 먼저 하게 함. 버퍼를 1번(stdout)에 넣음. Ioputs.c 에도 마찬가지로 write 를 추가.

3. glibc/build 폴더에서 ../configure –disable-sanity-checks 후 make, make install 완료.

4. hello.c 를 이용 해 gcc -c hello.c,  ar cr hello.a hello.o, gcc -o hello hello.c -static -L. -lc. 
